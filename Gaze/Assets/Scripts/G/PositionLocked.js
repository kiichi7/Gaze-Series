#pragma strict

function Start () {

}

function Update () {

/*	if (transform.position.x < -100) transform.position.x += 200;
	if (transform.position.y < -100) transform.position.y += 200;
	if (transform.position.z < -100) transform.position.z += 200;  */
}